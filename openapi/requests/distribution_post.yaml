components:
  schemas:
    request_distribution_post:
      type: object
      required:
        - name
        - path
      properties:
        name:
          type: string
          description: ディストリビューションの名前
          example: Ubuntu 2
        path:
          type: string
          description: インポート元のパス
          example: c:\wsl\exp1

